language: c
sudo: false
os:
  - linux
addons:
  apt:
    packages:
      - nasm
      - diffutils
      - libglew-dev
      - libglfw3
compiler:
  - gcc
cache:
  directories:
    - ffmpeg-samples
script:
  - mkdir -p ffmpeg-samples
  - ./configure --samples=ffmpeg-samples --cc=$CC
  - make -j 8
  - make fate-rsync
  - make check -j 8
